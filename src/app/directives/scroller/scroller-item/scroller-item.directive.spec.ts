import { ScrollerItemDirective } from './scroller-item.directive';

describe('ScrollerItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollerItemDirective();
    expect(directive).toBeTruthy();
  });
});
